# Read-only Postgres connection to the Eco Plumbers warehouse.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/ecoplumbersdb

# Port the server binds to (defaults to 8000 locally, 8080 in the container).
# PORT=8000
